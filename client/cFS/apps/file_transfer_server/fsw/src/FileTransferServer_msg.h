/* FileTransferServer_msg.h
 *
 * You could define server-specific messages here if needed.
 * However, in this example, we reuse the client's "request file" command struct,
 * so we only define server's internal structures if needed.
 */
#ifndef _FILETRANSFER_SERVER_MSG_H_
#define _FILETRANSFER_SERVER_MSG_H_

/* Potentially no new message structures needed if we reuse the client's request file command */

#endif /* _FILETRANSFER_SERVER_MSG_H_ */
